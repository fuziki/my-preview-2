import UIKit

// MARK: - UIColor + Asset

/// タイプセーフなアセットカラーアクセサ（swiftgen生成コードの代替としてAIが手書きしたもの）
public extension UIColor {
    /// カラーラベル「ピンク」用に定義した独自カラー
    static var customPink: UIColor {
        UIColor(named: "CustomPink", in: .module, compatibleWith: nil)!
    }
}
